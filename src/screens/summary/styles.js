import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({
    mainContainer: {
        flex: 1,
        backgroundColor: '#fff',
        borderColor: '#ccc',
        borderTopWidth: 1,
        padding: 20,
    },
    text: {
        fontSize: 24,
        fontWeight: 'bold',
        color: '#000'
    }
});

export default Styles;
